# QuickShare 개발 문서

이 문서 하나만 읽고도 프로젝트를 이어서 개발할 수 있도록 쓴 것이다.
무엇을 만들었는지, **왜 이 구조여야 했는지**, 그리고 만드는 과정에서 헛발질한 것까지 적었다.
설계 근거와 함정 부분을 특히 눈여겨볼 것. 코드만 봐서는 알 수 없는 내용이다.

---

## 1. 무엇을 만들었나

PC 를 파일 공유 서버로 만들어, **폰·태블릿의 브라우저만으로** 파일을 주고받는 도구.

- 앱 설치가 필요 없다. 상대 기기는 브라우저로 주소만 열면 된다
- PC 화면의 QR 을 찍으면 코드 입력도 없이 바로 연결된다
- **외부 서버를 전혀 거치지 않는다.** 같은 네트워크 안에서 직접 통신한다
- 4자리 코드로 잠근다. 같은 네트워크의 다른 사람은 못 들어온다

---

## 2. 왜 만들게 되었나

USB 케이블과 이메일 첨부가 귀찮다는 게 출발점이었지만,
기성 도구들이 **하나씩 다 막히면서** 직접 만드는 쪽으로 갔다. 그 과정이 곧 설계 근거다.

### 2-1. 사내망에 SSL 검사 장비가 있다

개발이 시작된 사무실 네트워크에는 HTTPS 를 복호화해서 들여다보는 보안 장비가 있었다.
포트는 열려 있고 TCP 연결도 정상이라 "연결이 안 된다"는 증상으로는 안 보인다.
인증서 발급자를 확인해야 드러난다.

```
Subject : CN=sfdataservice.microsoft.com, O=Microsoft Corporation
Issuer  : CN=<사내 보안장비> SSL, O=<장비 제조사>    <- 바꿔치기된 인증서
```

일반 브라우저는 회사 루트 인증서를 신뢰하도록 등록돼 있어 멀쩡히 돌아간다.
그러나 **인증서 고정(certificate pinning)** 을 쓰는 프로그램만 조용히 실패한다.
무한 로딩이나 끝나지 않는 업데이트로 나타난다.

- **Microsoft Store 앱 업데이트 전체가 막힌다**
- 그래서 **휴대폰과 연결(Phone Link)** 이 구버전(`0.23102.125.0`)에 묶여 영구 사용 불가
- `winget --source msstore` 도 `0x8a15005e` 로 실패
- 반면 일반 브라우저와 **npm 은 정상 동작한다** (회사 루트 인증서를 신뢰하므로)

> 함정: 이걸 "방화벽"이라고 부르면 IT 에 요청이 안 통한다. 포트는 이미 열려 있다.
> 정확한 요청은 해당 도메인을 **SSL 검사(복호화) 예외**에 넣어달라는 것이다.

### 2-2. Quick Share 는 하드웨어가 안 된다

대상 PC 는 유선 랜만 있고 **Wi-Fi 어댑터가 없다**(Realtek GbE 이더넷 단독).
Quick Share 는 실제 전송에 Wi-Fi Direct 를 쓰므로 블루투스만으로는 불가능하다.
게다가 설치를 Store 에서 해야 하는데 그게 2-1 때문에 막혀 있다.

### 2-3. PairDrop 은 구조 자체가 취약했다

브라우저 기반 P2P 도구. 결국 실패했고, **그 실패 원인이 이 프로젝트의 설계를 결정했다.**

| | PairDrop | QuickShare(이 프로젝트) |
| --- | --- | --- |
| 서로 찾는 법 | 외부 서버가 소개 | 주소를 직접 입력 / QR |
| 연결 유지 | 양쪽 탭이 **동시에 깨어 있어야** 함 | 유지하지 않음 |
| 전송 경로 | WebRTC P2P (STUN/TURN 협상) | 평범한 HTTP |
| 끊기는 지점 | 신호 서버, 탭 절전, 인증서 검사 | 없음 |
| 외부 의존 | 있음 | **없음** |

브라우저는 백그라운드 탭을 절전 상태로 만들면서 WebSocket 을 끊는다.
PC 를 보면 태블릿 탭이 자고, 태블릿을 보면 PC 탭이 잔다. 동시에 깨워둘 방법이 없었다.

**결론 — 상태를 유지하지 않는 구조로 간다.**
파일을 보낼 때만 잠깐 TCP 를 붙였다 끊는다. 유지하는 게 없으니 끊어질 것도 없다.

### 2-4. 결국 네트워크는 멀쩡했다

나중에 확인된 사실: PC 와 태블릿은 **같은 대역**이고
핑 0ms 로 라우터도 안 거친다. 사내망이 격리돼 있지 않았다.
즉 로컬 직접 통신이 처음부터 가능했고, 그게 가장 빠르고 안전한 길이었다.

---

## 3. 기술 스택

| 항목 | 값 | 이유 |
| --- | --- | --- |
| 런타임 | Node.js **v22.14.0** | 별도 프레임워크 없이 내장 `http` 만으로 충분 |
| 의존성 | **`qrcode` 하나** | QR 생성. 나머지는 전부 Node 내장 모듈 |
| 프런트 | 순수 HTML/CSS/JS 1파일 | 빌드 도구 없음. 파일 하나 열면 바로 수정 가능 |
| 대상 OS | Windows (bat 런처) | 서버 코드 자체는 OS 무관 |

빌드 단계가 없다. `npm install` 후 `node server.js` 가 전부다.

> `qrcode` 를 쓰기까지 QR 인코더를 직접 구현했다가 실패했다. 6-2 참고.

---

## 4. 파일 구조

```
quickshare-workspace/
├─ QuickShare 실행.bat   실행 진입점 (더블클릭)
├─ server.js             서버 전체 (라우팅·인증·업로드·QR)
├─ package.json
├─ public/
│  └─ index.html         화면 전체 (HTML+CSS+JS 한 파일)
├─ received/             상대 기기 -> 이 PC 로 온 파일
└─ share/                이 PC -> 상대 기기로 보낼 파일
```

`received/` 와 `share/` 는 **저장소가 아니라 세션용 임시 공간**이다. 8-3 참고.

---

## 5. 실행 방법

### 사용자

`QuickShare 실행.bat` 더블클릭. 그게 전부다.

1. Node 설치 여부 확인 → 없으면 안내하고 멈춤
2. **이미 실행 중이면** 서버를 또 띄우지 않고 브라우저만 엶
3. 처음이면 `npm install` 자동 실행
4. 서버 시작 → 2초 뒤 브라우저로 `http://localhost:4000` 자동 오픈
5. 콘솔에 QR·주소·코드 출력. 창을 닫거나 Ctrl+C 로 종료

그다음 **PC 화면의 QR 을 폰으로 찍으면** 끝이다.

### 개발자

```powershell
npm install
node server.js
```

| 환경 변수 | 기본값 | 설명 |
| --- | --- | --- |
| `PORT` | `4000` | 포트 |
| `PIN` | 매번 임의 생성 | 코드 고정용. 개발 중 재입력이 귀찮을 때만 |

---

## 6. 동작 흐름

### 6-1. 전체 그림

```
   폰·태블릿                   공유기/스위치                    PC
 192.168.0.31                       │                    192.168.0.12
      │                             │                          │
      │  ① QR 스캔                   │                          │
      │     http://192.168.0.12:4000/?code=1234                 │
      ├────────────────────────────►│─────────────────────────►│
      │                             │                     ② 코드 검증
      │◄────────────────────────────│◄─────────────────────────┤
      │     쿠키(토큰) 발급 + 화면     │                          │
      │                             │                          │
      │  ③ PUT /api/upload           │                          │
      │     (파일 본문 그대로)         │                          │
      ├────────────────────────────►│─────────────────────────►│
      │                             │                    received/ 에 기록
      │                             │                          │
      │  ④ GET /api/files (5초마다)   │                          │
      ├────────────────────────────►│─────────────────────────►│
      │◄────────────────────────────│◄─────────────────────────┤
      │     목록 JSON                │                          │
```

인터넷을 타지 않는다. 사무실 스위치 하나만 거친다.

### 6-2. 접속과 인증

1. 서버는 켜질 때 **4자리 코드를 새로 생성**한다 (`crypto.randomInt`)
2. QR 에는 코드가 포함된 주소가 담긴다 — `http://<랜IP>:4000/?code=1234`
3. 스캔하면 페이지가 `?code=` 를 읽어 `POST /api/auth` 로 인증하고,
   `history.replaceState` 로 주소창에서 코드를 지운다
4. 성공하면 **12시간짜리 토큰**을 쿠키(`qs_token`)로 받는다. 그 동안은 다시 안 묻는다
5. **PC 자신은 코드를 묻지 않는다.** 앞에 앉은 사람에게 코드를 묻는 건 의미가 없다

### 6-3. host / guest — 화면이 뒤집히는 구조

**이 프로젝트에서 가장 중요한 설계다.**

서버는 요청자의 IP 가 자기 주소(루프백 또는 자기 LAN IP)와 같은지 보고
**host**(서버를 돌리는 PC)와 **guest**(그 외 기기)를 가른다.

```js
function isHost(req) {
  if (!ownAddresses) ownAddresses = new Set(['127.0.0.1', '::1', ...lanAddresses()]);
  return ownAddresses.has(normalizeIp(req.socket.remoteAddress));
}
```

같은 폴더라도 **어느 쪽에서 보느냐에 따라 이름이 반대**가 된다.
한쪽이 보낸 것은 반대쪽에서 받은 것이므로.

| | PC 화면 (host) | 폰·태블릿 화면 (guest) |
| --- | --- | --- |
| 업로드 버튼 | 태블릿·폰으로 보내기 → `share/` | PC로 보내기 → `received/` |
| 받은 파일 | `received/` | `share/` |
| 보낸 파일 | `share/` | `received/` |
| 접속 코드 | 묻지 않음 | 필요 |
| 끌어다 놓기 안내 | 표시 | **숨김** |
| QR 카드 | 표시 | 숨김 (코드가 들어 있으므로) |

클라이언트의 `VIEW` 객체가 이 대칭을 그대로 담고 있다.
**새 문구를 추가할 일이 생기면 여기에 넣으면 된다.** 기기 판별을 따로 할 필요가 없다.

```js
const VIEW = {
  host:  { uploadTitle: '태블릿·폰으로 보내기', dropText: '여기에 끌어다 놓거나…',
           inbox: { box: 'received' }, outbox: { box: 'share' } },
  guest: { uploadTitle: 'PC로 보내기',          dropText: null,
           inbox: { box: 'share' },    outbox: { box: 'received' } },
};
```

### 6-4. 파일 전송

`multipart/form-data` 를 쓰지 않는다. **파일을 요청 본문에 그대로 실어 보낸다.**

```js
// 클라이언트 — 진행률을 받으려고 fetch 대신 XHR
xhr.open('PUT', '/api/upload?name=' + encodeURIComponent(file.name));
xhr.send(file);

// 서버 — 스트림을 그대로 디스크에 흘린다
req.pipe(fs.createWriteStream(dest));
```

파싱 코드가 없고, 큰 파일도 메모리에 올라가지 않는다.
파일명은 쿼리스트링으로 따로 넘긴다.

### 6-5. 목록 갱신과 도착 알림

폴링 방식이다. WebSocket 을 쓰지 않는 이유는 2-3 과 같다 — 유지할 연결을 만들지 않는다.

- **5초마다** `GET /api/files?box=...` 로 양쪽 목록을 갱신
- 업로드가 끝나면 5초를 기다리지 않고 **즉시 한 번 더** 갱신
- 이전에 보던 파일 이름 집합(`seen`)과 비교해 **새 이름이 생기면 도착**으로 판단
- 도착하면 `받은 파일` 탭이 강조색 배경으로 **3번 깜빡인다** (0.5초 × 3회)

> 화면이 가려져 있으면 브라우저가 프레임을 안 그려서 깜빡임이 재생되지 않은 채 소진된다.
> 그래서 `document.hidden` 이면 알림을 미뤄뒀다가 `visibilitychange` 때 재생한다.
> 폰은 화면이 수시로 꺼지므로 이 처리가 없으면 알림을 놓친다.

---

## 7. API

인증이 필요 없는 것은 `/api/session` 과 `/api/auth` 뿐이다. 나머지는 전부 토큰 검사.

| 메서드 | 경로 | 설명 |
| --- | --- | --- |
| GET | `/api/session` | `{ authed, role: 'host'\|'guest', connect }` — `connect` 는 host 에게만 |
| POST | `/api/auth` | `{ pin }` → 성공 시 `qs_token` 쿠키 발급 |
| GET | `/api/qr` | 접속용 QR (SVG). **host 에게만 내준다** — 코드가 들어 있으므로 |
| PUT | `/api/upload?name=` | 본문이 곧 파일. 저장 위치는 host/guest 에 따라 결정 |
| GET | `/api/files?box=` | `box` 는 `received` 또는 `share`. 최신순 목록 |
| GET | `/api/download?box=&name=` | 파일 내려받기 |

`box` 는 허용된 이름만 실제 경로로 바꾼다(`BOXES` 화이트리스트).
`?box=..\..\Windows` 같은 시도는 400 으로 막힌다.

---

## 8. 보안과 정책

### 8-1. 잠금

- 코드 비교는 해시 후 `crypto.timingSafeEqual` 로 한다
- 한 IP 에서 **10회 틀리면 10분간 차단**
- 업로드 파일명은 경로 요소와 Windows 금지 문자를 제거하고 저장(`safeName`)
- 다운로드는 정규화 후 대상 폴더 밖을 가리키면 거부

### 8-2. HTTP 평문이다

암호화하지 않는다. **신뢰할 수 있는 네트워크 안에서만 쓸 것.**
공용 와이파이에서는 쓰지 말 것. (자체 서명 HTTPS 는 브라우저 경고 때문에 QR 한 번에 여는 흐름을 망친다)

### 8-3. 재시작하면 두 폴더를 지운다

실행할 때마다 새 코드·새 QR 로 **새 세션**이 시작되므로,
시작 시점에 `received/` 와 `share/` 안의 파일을 **삭제**한다(`clearTransferFolders`).

- 하위 폴더는 건드리지 않는다 (`isFile()` 만 삭제)
- 화면에도 상시 경고를 띄운다 — "보관용이 아닙니다. 서버를 다시 켜면 이 목록은 모두 지워집니다."
- 남길 파일은 **서버를 끄기 전에** 다른 곳으로 옮겨야 한다

---

## 9. 삽질 기록 — 같은 실수를 반복하지 말 것

여기가 이 문서의 핵심이다. 아래 다섯 개는 전부 실제로 시간을 버린 것들이다.

### 9-1. 배치 파일: CP949 + CRLF 아니면 실행 자체가 안 된다

한글이 든 `.bat` 을 UTF-8/LF 로 저장하면 cmd 가 파싱하지 못한다.

| 증상 | 원인 |
| --- | --- |
| `'ttps:' is not recognized`, 한글 조각이 명령어로 잘림 | UTF-8 저장 |
| `The syntax of the command is incorrect` | 줄바꿈이 LF |

**반드시 CP949(시스템 ANSI) + CRLF 로 저장할 것.** 편집기가 UTF-8/LF 로 되돌리기 쉬우니 수정 후 매번 확인해야 한다.

```powershell
$p = 'QuickShare 실행.bat'
$cp949 = [System.Text.Encoding]::GetEncoding(949)
$t = [System.IO.File]::ReadAllText($p, $cp949) -replace "`r`n","`n" -replace "`n","`r`n"
[System.IO.File]::WriteAllText($p, $t, $cp949)
```

또 하나 — `chcp 65001` 은 **`node server.js` 직전에만** 건다.
node 의 UTF-8 출력(QR·한글)이 깨지지 않게 하기 위함인데,
**그 줄 아래로는 한글을 쓰면 안 된다.** 배치 파일 자체는 CP949 로 읽히기 때문이다.

### 9-2. QR 을 직접 구현하려다 실패했다

npm 이 막혔다고 **착각해서**(Store 가 막힌 걸 보고 넘겨짚었다. 실제로는 npm 정상)
QR 인코더를 직접 짰다. 리드-솔로몬, 마스크 평가까지 구현했는데 스캐너가 못 읽었다.

버그는 두 개였다.
1. **포맷 정보 비트 순서가 뒤집힘** — 상위 비트부터 넣어야 하는데 하위 비트부터 넣었다
2. 데이터 영역에도 별도 버그

문제는 **자체 검증이 전부 통과했다는 것.** 직접 짠 디코더가 같은 오해를 공유해서
인코더와 디코더가 서로 짜고 맞은 셈이 됐다. 규격대로 읽는 실제 스캐너만 실패했다.

> **교훈: 자기가 만든 것끼리 맞춰본 건 검증이 아니다.**
> 제3자 구현(여기서는 `jsqr`)으로 확인해야 한다. 그리고 라이브러리가 있으면 그냥 써라.

여담으로 **여백(quiet zone)은 최소 4모듈**이어야 한다. 좁으면 데이터가 멀쩡해도 스캐너가 코드를 못 찾는다.

### 9-3. 기기 판별을 UA/포인터로 하려다 두 번 틀렸다

"폰·태블릿에서는 끌어다 놓기가 안 되니 안내 문구를 바꾸자"는 요구에서 벌어진 일.

| 시도 | 왜 실패했나 |
| --- | --- |
| `(hover: none) and (pointer: coarse)` | **S펜**이 달린 갤럭시 탭은 스타일러스를 정밀 포인터로 보고한다 |
| User-Agent 에서 `Android` 찾기 | 삼성 인터넷의 **"데스크톱 버전으로 보기"** 가 켜져 있으면 `X11; Linux x86_64` 로 위장한다 |

정답은 **이미 갖고 있던 신호**였다. 6-3 의 `role`(host/guest)은 서버가 접속 IP 로 판별하므로
위조할 수 없고 처음부터 정확했다. `VIEW` 에 `dropText` 한 줄 넣는 것으로 끝났다.

> **교훈: 새 판별 수단을 만들기 전에 이미 정확하게 동작하는 신호가 있는지 먼저 보라.**

### 9-4. 브라우저 패널이 숨겨져 있으면 CSS 애니메이션이 재생되지 않는다

개발 중 깜빡임을 테스트할 때, 클래스도 붙고 `getAnimations()` 도 `running` 인데
`animationstart` 가 안 왔다. 원인은 `document.visibilityState === 'hidden'`.
페이지가 프레임을 안 그리면 애니메이션은 등록만 되고 진행하지 않는다.

이건 테스트 환경 문제이자 **실사용 문제**이기도 하다. 6-5 의 지연 재생이 그래서 들어갔다.

### 9-5. `getComputedStyle` 은 라이브 객체다

```js
tab.classList.add('alert');
const cs = getComputedStyle(tab);   // 스냅샷이 아니다
tab.classList.remove('alert');
cs.animationDuration;               // "0s" — 지운 뒤의 값을 읽는다
```

값을 비교하려면 그 자리에서 원시값으로 복사해둬야 한다.

---

## 10. 앞으로 할 것

- [ ] **부팅 시 자동 시작** — `shell:startup` 에 bat 바로가기. 창이 거슬리면 속성에서 `최소화`
- [ ] **`.exe` 패키징** — Node 가 없는 사람도 쓰게. Node SEA 또는 pkg
- [ ] **GitHub 공개** — 각자 자기 PC 에서 돌리는 구조라 서버 운영비도, 남의 파일을 떠안을 일도 없다
- [ ] 목록이 길어질 때의 정렬·검색
- [ ] 여러 기기가 동시에 붙었을 때의 표시 (현재는 기기 구분 없이 폴더 하나를 공유)

### 손대지 말아야 할 것

- **외부 서버 경유로 바꾸지 말 것.** 이 프로젝트의 존재 이유가 사라진다 (2장 전체)
- **WebRTC/WebSocket 으로 바꾸지 말 것.** PairDrop 이 실패한 지점이다 (2-3)
- **기기 판별 로직을 다시 만들지 말 것.** `role` 로 충분하다 (9-3)
